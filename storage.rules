rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Umožní čtení a zápis pouze přihlášeným uživatelům do jejich vlastních souborů
    match /sources/{lessonId}/{fileName} {
      allow read, write: if request.auth != null;
    }
  }
}